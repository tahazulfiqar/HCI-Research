import React from "react";
import { Container, ListGroup, ListGroupItem, Row } from "reactstrap";

// const commentMap = comments => {
//   return comments.map(comment => <ListGroupItem>{comment}</ListGroupItem>);
// };

// const commentList = comments => (
//   <Container>
//     <ListGroup>{commentMap(comments)}</ListGroup>
//   </Container>
// );

export const PhotoList = entities => (
  <Row>
    <div
      style={{
        width: "100%",
        fontSize: "22"
      }}
    >
      <Container>something</Container>
    </div>
  </Row>
);
